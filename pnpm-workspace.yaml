packages:
  - "packages/*"
  - "apps/*"

# pnpm 11 build approval (allowBuilds replaced onlyBuiltDependencies).
# esbuild (tsup + vite) needs its postinstall on fresh installs (CI, Render).
allowBuilds:
  esbuild: true
